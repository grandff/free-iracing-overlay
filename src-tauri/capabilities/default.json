{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Both windows need core events: the HUD receives the Alt+J toggle, and the control window receives iRacing connection changes.",
  "windows": ["main", "control"],
  "permissions": ["core:default"]
}
